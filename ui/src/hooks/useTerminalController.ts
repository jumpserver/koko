
// Terminal 的创建（事件、默认配置）、修改终端主题


/**
 * @description 终端控制器
 * @param config 
 */
export const useTerminal = (config: any) => {

  /**
   * @description  创建终端实例
   */
  const createTerminalInstance = () => {} 

  /**
   * @description 设置终端主题
   */
  const setTerminalTheme = () => {

  }

  return {
    setTerminalTheme,
    createTerminalInstance
  }
}
